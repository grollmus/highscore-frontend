import { AppValueDirective } from './value.directive';

describe('ShowScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new AppValueDirective();
    expect(directive).toBeTruthy();
  });
});
