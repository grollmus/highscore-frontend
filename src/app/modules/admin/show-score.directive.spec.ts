import { ShowScoreDirective } from './show-score.directive';

describe('ShowScoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowScoreDirective();
    expect(directive).toBeTruthy();
  });
});
